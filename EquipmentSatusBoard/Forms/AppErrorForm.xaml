<Window x:Class="EquipmentSatusBoard.Forms.AppErrorForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EquipmentSatusBoard.Forms"
        mc:Ignorable="d"
        Title="AppErrorForm" Background="{x:Null}" AllowsTransparency="True" WindowStyle="None" ResizeMode="NoResize" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterOwner">
    <Grid>
        <Rectangle Style="{StaticResource DialogbackGround}"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="25">
            <Image Height="100" Width="100" Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center" Source="/Equipment Satus Board;component/Error.png"/>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
                <Label Style="{StaticResource DialogTitle}" Content="Something went wrong!" HorizontalAlignment="Center"/>
                <Label Name="errorMessage" HorizontalAlignment="Center" FontWeight="Bold"/>
                <Button Content="Esc to close" HorizontalAlignment="Center" Background="{x:Null}" BorderBrush="{x:Null}" IsCancel="True"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
